"""claude-chat — a chat surface for headless Claude Code sessions.

Every conversation is a ``claude --print`` session pinned to a directory: pick
a saved workspace or type any path, and the session boots there. Localhost
daemon, single-page UI, live turn telemetry, mid-flight steering, Allow/Deny
approval cards. No external app, no tokens, no cloud.
"""

__version__ = "0.1.0"
